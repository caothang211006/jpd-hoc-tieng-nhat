# Bản text giải mã từ PDF của nhà xuất bản — dùng để đối chiếu khi soạn dữ liệu,
# không đẩy lên repo công khai vì đây gần như là bản sao nguyên vẹn tài liệu gốc.
# Bỏ dòng dưới nếu bạn để repo ở chế độ private và muốn giữ kèm.
reference/
